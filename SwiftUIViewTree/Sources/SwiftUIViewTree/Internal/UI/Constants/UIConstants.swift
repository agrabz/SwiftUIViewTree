
import Foundation
import SwiftUI

enum UIConstants {
    enum ScreenRatioOf {
        static let originalContent: CGFloat = 1/4
        static let viewTree: CGFloat = 3/4
    }

    enum Color {
        static let collapsedNodeBackground = SwiftUI.Color.gray.opacity(0.8)
        static let initialNodeBackground = SwiftUI.Color.purple.opacity(0.8)
    }
}
